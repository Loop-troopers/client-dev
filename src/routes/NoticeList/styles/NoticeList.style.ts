import styled from "styled-components";

export const Container = styled.div`
  width: 100%;
  padding: calc(var(--top-navbar-height) * 2) 1rem
    calc(var(--btm-navbar-height) * 2);
  background-color: var(--main-bg-color);
  z-index: 0;
`;
